# Si on veut utiliser un package externe, il faut d'abord l'installer dans notre environnment (ex: venv)
# On peut installer à partir de
#   1. la vue de PyCharm (utiliser la vue en bas à gauche "python packages"
#   2. Erreurs de PyCharm (click droit et utiliser l'action)
#   3. ligne de commande: pip install <nom package>
import sounddevice
import pyglet
from bs4 import BeautifulSoup

